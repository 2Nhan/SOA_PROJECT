<!DOCTYPE html>
<html>
<head>
  <title>RFQ Requests - Supplier</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <%- include('partials/nav') %>
  <div class="container mt-4">
    <h2>Incoming RFQ Requests</h2>
    <% if (rfqs.length === 0) { %>
      <div class="alert alert-info">No RFQ requests received yet.</div>
    <% } else { %>
    <table class="table table-hover mt-3">
      <thead class="table-dark">
        <tr><th>#</th><th>Product</th><th>Shop</th><th>Qty</th><th>Status</th><th>Date</th><th>Actions</th></tr>
      </thead>
      <tbody>
        <% rfqs.forEach(r => { %>
        <tr>
          <td><%= r.id %></td>
          <td><%= r.product_name %></td>
          <td><%= r.shop_name %></td>
          <td><%= r.quantity %></td>
          <td>
            <% if (r.status === 'pending') { %><span class="badge bg-warning">Needs Quote</span>
            <% } else if (r.status === 'quoted') { %><span class="badge bg-info">Quoted</span>
            <% } else if (r.status === 'accepted') { %><span class="badge bg-success">Accepted</span>
            <% } else { %><span class="badge bg-secondary"><%= r.status %></span><% } %>
          </td>
          <td><%= new Date(r.created_at).toLocaleDateString() %></td>
          <td><a href="/admin/rfqs/<%= r.id %>" class="btn btn-sm btn-primary">View</a></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
    <% } %>
  </div>
</body>
</html>
