<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/admin/">B2B Marketplace - Supplier</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-nav">
        <a class="nav-link" href="/admin/">Dashboard</a>
        <a class="nav-link" href="/admin/products">Products</a>
        <a class="nav-link" href="/admin/rfqs">RFQs</a>
        <a class="nav-link" href="/admin/contracts">Contracts</a>
        <a class="nav-link" href="/admin/orders">Orders</a>
        <% if (typeof currentUser !== 'undefined' && currentUser && currentUser.role === 'admin') { %>
          <a class="nav-link text-warning" href="/admin/manage">Admin</a>
        <% } %>
        <a class="nav-link" href="/">Shop View</a>
      </div>
      <div class="navbar-nav ms-auto">
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
          <span class="nav-link text-info"><%= currentUser.role === 'admin' ? '🛡️' : '📦' %> <%= currentUser.full_name %></span>
          <a class="nav-link" href="/admin/profile">Profile</a>
          <a class="nav-link text-warning" href="/admin/logout">Logout</a>
        <% } else { %>
          <a class="nav-link" href="/login">Login</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
