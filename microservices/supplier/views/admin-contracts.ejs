<!DOCTYPE html>
<html>
<head>
  <title>All Contracts - Admin</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <%- include('partials/nav') %>
  <div class="container mt-4">
    <h2>All Contracts (System Overview)</h2>
    <a href="/admin/manage" class="btn btn-sm btn-secondary mb-3">Back to Admin Dashboard</a>
    <% if (contracts.length === 0) { %>
      <div class="alert alert-info">No contracts in the system yet.</div>
    <% } else { %>
    <table class="table table-hover">
      <thead class="table-dark">
        <tr><th>#</th><th>Product</th><th>Shop</th><th>Supplier</th><th>Qty</th><th>Total</th><th>Status</th><th>Date</th></tr>
      </thead>
      <tbody>
        <% contracts.forEach(c => { %>
        <tr>
          <td><%= c.id %></td>
          <td><%= c.product_name %></td>
          <td><%= c.shop_name %></td>
          <td><%= c.supplier_name %></td>
          <td><%= c.quantity %></td>
          <td>$<%= parseFloat(c.total_amount).toFixed(2) %></td>
          <td>
            <% if (c.status === 'draft') { %><span class="badge bg-warning">Draft</span>
            <% } else if (c.status === 'confirmed') { %><span class="badge bg-success">Confirmed</span>
            <% } else if (c.status === 'completed') { %><span class="badge bg-primary">Completed</span>
            <% } else { %><span class="badge bg-danger">Cancelled</span><% } %>
          </td>
          <td><%= new Date(c.created_at).toLocaleDateString() %></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
    <% } %>
  </div>
</body>
</html>
